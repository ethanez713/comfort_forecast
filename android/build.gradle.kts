// Root build file. Versions are a known-good, widely-compatible combo;
// Android Studio may offer to upgrade the Android Gradle Plugin — that's fine, accept it.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
